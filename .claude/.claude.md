# Gully Project - Claude Code Context

## Project Overview
**Gully** is a sports challenge platform MVP being built by a solo developer learning Claude Code mastery.

**Timeline:** 90 minutes/day | **Budget:** $0 MVP | **Goal:** Demo-ready app for investors/users

---

## Critical Constraints (READ FIRST)

### Team & Resources
- Solo developer with MacBook + Claude Pro plan
- Zero cloud budget until validation/funding
- 90 minutes daily development time
- Weekly reviews to track progress and token usage

### Primary Goal
Build working MVP with **minimal/zero cost** to demonstrate to investors and early users while mastering Claude Code.

---

## Technology Stack - LOCAL-FIRST APPROACH

### ✅ APPROVED Stack
- **Backend:** Node.js + Express **MONOLITH** (NOT microservices)
- **Database:** SQLite (dev) → PostgreSQL via Docker Compose (local)
- **Cache:** Redis via Docker Compose (local)
- **Frontend:** React Native with Expo OR Next.js web app
- **Auth:** Passport.js + JWT (self-hosted) or Supabase free tier
- **Hosting:** Render/Railway/Fly.io free tiers, ngrok for demos
- **Storage:** Local filesystem → free tier cloud when needed

### ❌ FORBIDDEN Until Funded
- AWS services beyond free tier
- Auth0, Firebase Auth (paid tiers)
- Stripe, SendGrid, Twilio, Mux
- Any monthly subscription services
- Kubernetes, complex orchestration
- Distributed microservices (modular monolith is OK)

---

## Architecture: MODULAR MONOLITH (Hybrid Approach)

**Strategy:** Build as a **modular monolith** that can evolve into microservices when needed.

### Why Modular Monolith?
- ✅ Start fast with single deployment (monolith benefits)
- ✅ Clear service boundaries (microservices benefits)
- ✅ Easy local development, zero cloud costs
- ✅ Smooth migration path to microservices when scaling
- ✅ Each module is independently testable and maintainable
- ✅ Can extract services to containers later without rewrites

### The Best of Both Worlds
Each module (user, team, match, etc.) is:
- Structured like a microservice (own controllers, models, routes)
- Self-contained with clear interfaces
- But runs in the **same Node.js process** initially
- Can be extracted to separate Docker container when needed

### Structure
```
backend/
  ├── src/
  │   ├── services/           # Structured like microservices
  │   │   ├── user-service/
  │   │   │   ├── controllers/
  │   │   │   ├── models/
  │   │   │   ├── routes/
  │   │   │   ├── services/
  │   │   │   ├── validators/
  │   │   │   └── index.ts    # Service entry point
  │   │   ├── team-service/
  │   │   │   ├── controllers/
  │   │   │   ├── models/
  │   │   │   ├── routes/
  │   │   │   └── index.ts
  │   │   ├── match-service/
  │   │   │   └── ... (same structure)
  │   │   └── stats-service/
  │   │       └── ... (same structure)
  │   ├── shared/             # Shared utilities
  │   │   ├── database/       # DB connection & migrations
  │   │   ├── middleware/     # Auth, validation, error handling
  │   │   ├── types/          # TypeScript interfaces
  │   │   ├── utils/          # Helper functions
  │   │   └── config/         # Environment config
  │   ├── api-gateway/        # Optional: route aggregation
  │   │   └── routes.ts       # Maps routes to services
  │   └── app.ts              # Main application entry
  ├── tests/
  │   ├── unit/
  │   └── integration/
  ├── docker-compose.yml      # Local PostgreSQL, Redis
  ├── Dockerfile              # Single container (for now)
  ├── package.json
  └── tsconfig.json
```

### Migration Path to True Microservices
When you need to scale (post-funding/validation):
```
Phase 1: Modular Monolith (NOW)
  → Single Node.js process
  → Shared database connection
  → Fast iteration

Phase 2: Containerized Services (LATER)
  → Each service gets own Dockerfile
  → docker-compose orchestrates multiple containers
  → Still runs locally or on cloud

Phase 3: Cloud Microservices (FUTURE)
  → Deploy to AWS ECS/Fargate
  → Separate databases per service
  → API Gateway, message queues
  → Full distributed architecture
```

---

## MVP Scope - Phase 1 ONLY

### ✅ IN SCOPE (Must Have)
1. User auth (email/password signup/login)
2. User profiles (name, avatar, bio, sports)
3. Team creation & member management
4. Challenge system (send/accept/decline)
5. Match scheduling & result submission
6. Basic stats (win/loss, match history)
7. Simple leaderboard

### ❌ OUT OF SCOPE (Defer to v2+)
- Leagues & tournaments
- Video highlights
- Marketplace
- Social feed/following
- Push notifications (use email)
- Advanced analytics
- Payment processing
- Mobile apps (web first)

### Strategic Decisions
- **Single sport first:** Cricket OR Football only
- **Web before mobile:** Faster demos, no app store approval
- **Manual admin:** Handle disputes/moderation manually
- **Email notifications:** Transactional emails vs push infrastructure

---

## Weekly Review Checklist

Run every Sunday or end-of-week:

### 1. Token Consumption
- [ ] Track total tokens used this week
- [ ] Identify wasteful prompts or hallucination loops
- [ ] Optimize context usage (remove unnecessary files)
- [ ] Refine prompting strategy for next week

### 2. Progress Assessment
- [ ] Features completed vs planned
- [ ] Blockers identified and documented
- [ ] Velocity check: on track for 12-week MVP?
- [ ] Adjust next week's scope if needed

### 3. Learning Documentation
- [ ] Document what worked (successful Claude patterns)
- [ ] Document what failed (hallucinations, bugs)
- [ ] Log reusable patterns/prompts
- [ ] Context management lessons learned

### 4. Hallucination Prevention
- [ ] Review incorrect AI suggestions from the week
- [ ] Update verification checklist
- [ ] Flag complexity red flags encountered
- [ ] Strengthen "question everything" discipline

### 5. Cost Tracking
- [ ] Confirm zero-spend maintained
- [ ] Check free tier usage vs limits
- [ ] Flag any payment-required suggestions
- [ ] Document workarounds for paid services

### 6. Context Management
- [ ] Remove stale files from Claude context
- [ ] Archive completed feature branches
- [ ] Clean up TODO lists
- [ ] Verify .claude.md is current

---

## Development Principles

### 1. Local-First Development
All development runs on MacBook. No cloud dependencies for MVP. Docker Compose for services.

### 2. Monolith Strategy
Single codebase with modular folders. Easier debugging. Lower overhead. Refactor later.

### 3. Pragmatic MVP
Ship working features > perfect architecture. Manual processes OK. Technical debt acceptable if it delivers value.

### 4. Avoid Hallucinations
- Verify every AI suggestion before accepting
- Test immediately after code generation
- Question unnecessary complexity
- **Trust but verify always**

### 5. Cost Consciousness
Challenge any tool/service that costs money. Use free tiers and open source. Delay paid services until revenue/funding.

### 6. Learning Focus
Understand code, don't just copy. Document patterns. Build mental models. Prioritize knowledge over speed.

---

## Anti-Patterns - ACTIVELY AVOID

### 🚫 Premature Microservices
**Wrong:** 6 separate services for 0 users
**Right:** Monolith with clear module boundaries

### 🚫 Complex Infrastructure
**Wrong:** Kubernetes, message queues, event buses
**Right:** Docker Compose locally, simple hosting

### 🚫 Paid Services Too Early
**Wrong:** Monthly costs before validation
**Right:** Free tiers, self-hosted, manual processes

### 🚫 Over-Documentation
**Wrong:** Perfect docs instead of working code
**Right:** Code comments + basic README, ship first

### 🚫 Building for Scale
**Wrong:** Optimizing for 1M users at 0 users
**Right:** Build for 100 users, refactor when proven

### 🚫 Parallel Claude Instances Too Early
**Wrong:** Managing multiple contexts before mastering one
**Right:** Master single workflow in weeks 1-4 first

---

## Claude Code Interaction Guidelines

### When Claude Suggests Something, Ask:
1. **Does this cost money?** → Challenge if yes
2. **Is this over-engineered?** → Simplify for MVP
3. **Can we test this immediately?** → Verify before proceeding
4. **Do we need this for v1?** → Defer if not core
5. **Is there a simpler approach?** → Always seek simplicity

### Optimal Prompting
- **Be specific:** "Create user login endpoint" not "setup auth"
- **Set constraints:** "Using Passport.js, no paid services"
- **Request testing:** "Include test cases for this function"
- **Limit scope:** "Just the signup route, not the whole auth system"
- **Verify understanding:** "Confirm this uses SQLite, not cloud DB"

### Context Management
- **Include only relevant files** in Claude's context
- **Archive completed work** to reduce token usage
- **Use focused sessions:** One feature at a time
- **Clear TODO lists** after completion

---

## 12-Week MVP Timeline

### Weeks 1-2: Foundation
- Monolith project structure
- SQLite database + migrations
- Docker Compose setup
- Basic auth with Passport.js

### Weeks 3-4: User & Team Features
- User profiles CRUD
- Team creation/management
- Member invitations

### Weeks 5-6: Challenge System
- Challenge creation flow
- Accept/decline logic
- Match scheduling

### Weeks 7-8: Match Management
- Result submission
- Verification workflow
- Match history

### Weeks 9-10: Stats & UI
- Win/loss tracking
- Leaderboard
- UI polish

### Weeks 11-12: Launch Prep
- Bug fixes
- Deploy to free hosting
- Demo data seeding
- Investor demo ready

---

## Success Metrics (Week 12 Checkpoint)

### Technical
- ✅ Deployed on free hosting (Render/Railway)
- ✅ 5 core features working end-to-end
- ✅ Zero monthly costs
- ✅ <500ms API response times
- ✅ Mobile-responsive web app

### Learning
- ✅ Mastered Claude Code workflow
- ✅ Can debug AI code independently
- ✅ 20+ documented reusable patterns
- ✅ 50% token usage reduction from week 1

### Product
- ✅ Full demo flow: signup → challenge → match → results
- ✅ 10+ test users completed flows
- ✅ Feedback collected for v2
- ✅ Investor-ready demo

---

## Key Mantras (Repeat Often)

1. **"Zero cost until validated"** - No paid services without users
2. **"Modular monolith, always"** - Service structure, single process
3. **"Ship, then polish"** - Working beats perfect
4. **"Verify everything"** - Trust but test AI code
5. **"Learn by building"** - Understanding over speed
6. **"Local-first forever"** - MacBook is the datacenter

---

## Current Week Focus

**Week 1:** Foundation setup - see detailed tasks below

---

## Week 1 Detailed Plan (Revised for Monolith)

### Day 1 (90 min): Project Structure
```bash
# Create modular monolith structure
mkdir -p backend/src/services/{user-service,team-service,match-service,stats-service}
mkdir -p backend/src/shared/{database,middleware,types,utils,config}
mkdir -p backend/src/api-gateway
mkdir -p backend/tests/{unit,integration}
mkdir -p frontend
mkdir -p docs
# Create package.json, tsconfig.json, .gitignore
# Organize existing docs into proper folders
# Commit: "feat: initialize modular monolith project structure"
```

### Day 2 (90 min): Database Setup
- SQLite database file
- Migration framework (node-pg-migrate or Knex)
- Users table migration
- Test database connection
- Commit: "feat: setup SQLite database with migrations"

### Day 3 (90 min): Auth Foundation
- Install Passport.js, bcrypt, jsonwebtoken
- POST /auth/signup endpoint
- POST /auth/login endpoint
- JWT generation utility
- Commit: "feat: implement basic authentication"

### Day 4 (90 min): User Module
- GET /users/:id endpoint
- PUT /users/:id endpoint
- User profile model
- Basic validation
- Commit: "feat: add user profile management"

### Day 5 (90 min): Team Module Scaffold
- POST /teams endpoint (create team)
- GET /teams/:id endpoint
- Team model with captain_id
- Commit: "feat: scaffold team creation"

### Day 6 (90 min): Docker Compose
- docker-compose.yml (PostgreSQL, Redis for future)
- Switch from SQLite to PostgreSQL
- Test database connectivity
- Commit: "feat: add Docker Compose for local services"

### Day 7 (90 min): Testing & Review
- Jest setup
- 2-3 basic tests (auth, user endpoints)
- Weekly review (document in WEEK1_REVIEW.md)
- Commit: "feat: add testing framework and week 1 review"

---

## Notes for Claude

- **Always assume monolith** unless explicitly told otherwise
- **Challenge paid services** - suggest free alternatives
- **Keep it simple** - MVP means minimum viable
- **Test immediately** - don't accumulate untested code
- **Document learnings** - help human track patterns
- **Question complexity** - simpler is better

---

**Last Updated:** Week 1, Day 1
**Next Review:** End of Week 1 (Day 7)
