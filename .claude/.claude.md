# Gully - Claude Code Context

## 🎯 Project Identity
**Solo developer** | Sports challenge MVP | **$0 budget** | **Phase 2/7** (User Profiles)
Phase 1 ✅ Complete (121 tests, 90%+ coverage) → Phase 2 in progress

---

## 📊 Task Tracking

**Source of Truth:** `tools/tracker/data/TASK_TRACKER.csv`
**Dashboard:** `cd tools/tracker && ./launch-dashboard.sh`

### 🔒 TRACKER PROTOCOL (NON-NEGOTIABLE)

**Session Startup:**
1. Check task status via SlashCommand tool (gullystatus) → reads CSV, identifies pending work
2. Confirm task with user
3. Update CSV: `status=in_progress`, `started=YYYY-MM-DD`

**Completing Task:**
- Update CSV: `status=completed`, `completed=YYYY-MM-DD`, `test_count=N`, `notes=summary`
- Commit CSV with task completion

**Validation:**
- ALWAYS check status at session start using SlashCommand tool
- NEVER assume task status from memory
- ALWAYS update CSV when completing tasks

---

## 🚨 FILE BUDGET LIMITS (HOOK-ENFORCED)

**Every file in .claude/ loads at EVERY session = $$$ cost**

**Before writing docs/commands**: Check `.claude/FILE_BUDGETS.json` for limits. Hook auto-enforces.

**Strict limits** (writes BLOCKED if exceeded):
- `.claude/commands/*.md`: 30 lines (logic only, NO examples)
- `.claude/agents/*.md`: 150 lines (instructions only, NO background)
- `.claude/.claude.md`: 200 lines (this file)
- `docs/sessions/*.md`: 50 lines (use `/gullycondense`)

**Rule**: If it doesn't need to load every session → move to `docs/`

**Directory Rules:**
- Project docs → `/docs/`
- Context (on-demand) → `/docs/context/`
- Agent prompts → `/.claude/agents/` (concise) | verbose → `/docs/agents/`
- Commands → `/.claude/commands/` (concise) | docs → `/docs/commands/`

**Hook blocks violations.** Emergency bypass: `CLAUDE_HOOK_BYPASS=1` (rare only)

---

## 📂 File Discovery

**ALWAYS use INDEX files first:**
- Root: `INDEX.md`
- Context: `docs/context/INDEX.md`
- Tasks: `tools/tracker/data/tasks/index.json`

Never use Glob/Grep before checking INDEX. Context loaded on-demand via SlashCommand tool (gullycontext).

---

## 💬 Session Startup

**MUST:**
1. Read `.claude/NEXT_SESSION.md` if exists (continuation from previous session)
2. State context (2-3 sentences): "Phase X working on [task]"
3. Load current task state via SlashCommand tool (gullystatus)
4. Load context on-demand via SlashCommand tool (gullycontext) with sections:
   - `mvp` - MVP scope | `arch` - Architecture | `database` - Schema | `learnings` - Phase 1 patterns

---

## 🔄 Task Workflow

1. State the Goal - Confirm task
2. State the Approach - High-level method + steps
3. State First Step - Immediate action
4. Enter Plan Mode ONLY if ambiguous or major architectural changes

---

## 🤖 PM Agent Workflow

**PM Orchestrator coordinates:**
- Task Generator → Breaks plans into tasks
- Task Executor → Executes tasks, updates tracker

**Use Task tool with:**
- `subagent_type: pm-agent-initialize` - Initial project setup
- `subagent_type: pm-agent-orchestrator` - Ongoing development coordination

---

## 💬 Communication Protocol

**Claude MUST:**
- Ask questions frequently (don't assume)
- Confirm requirements before complex features
- Request context when needed
- Flag uncertainties immediately

**Philosophy:** Frequent communication > Silent assumptions

---

## 🏗️ Architecture (Non-Negotiable)

**MODULAR MONOLITH** (NOT Microservices)
- Service-oriented structure, single Node.js process
- Clear boundaries, runs as monolith

**Stack:**
- Backend: Node.js + Express + TypeScript
- Database: PostgreSQL + Redis (Docker, local)
- Auth: Passport.js + JWT (self-hosted)
- Frontend: Next.js (Week 3+)

**Service Boundaries:**
- User: `/api/auth/*`, `/api/users/*`
- Team: `/api/teams/*`
- Match: `/api/challenges/*`, `/api/matches/*`
- Stats: `/api/stats/*`

**Details:** Load via SlashCommand tool (gullycontext arch)

---

## 🚫 Non-Negotiables

**❌ FORBIDDEN:** Paid services | Kubernetes | Microservices | Over-engineering
**✅ REQUIRED:** TDD (90%+ coverage) | Zero-cost dev | Immediate verification | Small chunks (60-90 min)

---

## 🔧 Git Workflow (CRITICAL - ENFORCED BY HOOK)
**Task Complete → PR → Merge → New Branch:** 1) Create PR to parent 2) Merge 3) `git checkout develop && git pull` 4) `git checkout -b feature/P{Phase}-{Component}-T{Task}` for NEXT task
**Auto-commit:** Commit on completion (conventional format + Co-Authored-By) | NEVER ask unless secrets/force-push
**NEVER VIOLATE:** Start new task on old branch | Commit to master/develop | Skip PR workflow
**Hook enforces:** Branch naming `feature/P*-*-T*` | Blocks commits if on wrong branch for task

---

## 📋 Code Standards

**Files:** `*.controller.ts` | `*.routes.ts` | `*.test.ts`

**Imports:**
```typescript
import { db } from '@shared/database';  // Absolute for shared
import { UserController } from './controllers/user.controller';  // Relative within service
```

---

## 🎯 Core Principles

1. **Zero cost until validated** - No paid services without users
2. **Modular monolith, always** - Service structure, single process
3. **Ship, then polish** - Working beats perfect
4. **Verify everything** - Tests + build before moving on
5. **Local-first forever** - MacBook is datacenter
6. **Communicate constantly** - Questions > Assumptions
7. **TDD discipline** - Tests first, 90%+ coverage

---

**Last Updated:** 2025-11-16 (Budget enforcement + condensed to 200 lines)
